[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && sh scripts/railway-start.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
healthcheckPath = "/health"
healthcheckTimeout = 60
