[build]
builder = "nixpacks"
buildCommand = "npm install --prefix frontend && npm run build --prefix frontend && cp -r frontend/dist backend/public && npm install --prefix backend"

[deploy]
startCommand = "cd backend && npx prisma migrate deploy && node src/server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
